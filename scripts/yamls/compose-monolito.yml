version: '3.8'

services:
  java-app:
    image: monolito:latest
    container_name: java-app-monolito
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m -Xms256m
    restart: always
